<%= form_with(model: @building, local: true) do |form| %>
  <% if building.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(building.errors.count, "error") %> prohibited this building from being saved:</h2>

      <ul>
        <% building.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :build, '物件名'%>
    <%= form.text_field :build %>
  </div>

  <div class="field">
    <p><%= form.label :price, '賃料' %>
    <%= form.text_field :price %>円</p>
  </div>

  <div class="field">
    <%= form.label :address, '住所' %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <p><%= form.label :age ,'築年数' %>
    <%= form.text_field :age %></p>
  </div>

  <div class="field">
    <%= form.label :remarks, '備考' %>
    <%= form.text_area :remarks %>
  </div>

<hr>

<%= form.fields_for :stations, local: true do |station| %>
  <div class="field">
    <h3>最寄り駅</h3>
    <%= station.label :line, '路線名' %>
    <%= station.text_field :line %>
  </div>
  <div class="field">
    <%= station.label :station, '駅名' %>
    <%= station.text_field :station %>
  </div>
    <div class="field">
      <p><%= station.label :distance, '徒歩分数' %>
      <%= station.text_field :distance %>分</p>
  </div>
  <% end %>
  <div class="actions">
    <%= form.submit '登録する' %>
  </div>
<% end %>
